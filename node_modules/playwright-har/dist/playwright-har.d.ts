import { Page } from 'playwright-chromium';
import { PlaywrightHarConfig } from './playwright-har-config';
export declare class PlaywrightHar {
    private page;
    private client;
    private addResponseBodyPromises;
    private events;
    private config;
    constructor(page: Page, config?: PlaywrightHarConfig);
    start(): Promise<void>;
    stop(path?: string): Promise<any>;
}
